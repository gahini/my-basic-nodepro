'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // Baseline migration - existing tables managed by migrations from now on
  },

  down: async (queryInterface, Sequelize) => {
    // Baseline migration - no rollback
  }
};
